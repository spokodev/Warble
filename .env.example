# Sentry — set at build time so Vite bakes the DSN into the bundle.
# Project: spokodev-lh/spoko-whisper (https://sentry.io/organizations/spokodev-lh/)
# Sentry init lives in src/main.tsx and is disabled when DSN is unset (dev).
VITE_SENTRY_DSN=
VITE_GIT_COMMIT_SHA=
